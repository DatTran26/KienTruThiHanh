import { createClient } from '@supabase/supabase-js'; const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function main() { const adminRes = await supabase.auth.admin.createUser({ email: 'admin@hcm.gov.vn', password: 'password123', email_confirm: true, user_metadata: { role: 'admin' } }); console.log('Admin:', adminRes.error ? adminRes.error.message : 'Created '+adminRes.data.user.email); const userRes = await supabase.auth.admin.createUser({ email: 'canbo@hcm.gov.vn', password: 'password123', email_confirm: true, user_metadata: { role: 'user' } }); console.log('User:', userRes.error ? userRes.error.message : 'Created '+userRes.data.user.email); } main();
